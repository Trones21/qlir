
df_materialization/
├── registry.py        # state: what is registered
├── registrar.py       # primitive operation: how to register one thing
├── registration.py   # control plane: what gets registered
├── materialize.py    # runtime execution: build dfs
